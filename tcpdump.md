---
title: tcpdump
layout: default
parent: Linux
---

# tcpdump

tcpdump is a network protocol analyzer.

```shell
tcpdump -v
```

```shell
tcpdump -i eth0
```

```shell
tcpdump -i wlan0 'port = 80'
```